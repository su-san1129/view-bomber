[tools]
bun = "1.3.10"
node = "22"
rust = "1.93.0"

[tasks.init]
description = "Install dependencies and provision bundled PDF export binaries"
run = "bun run init"

[tasks.tauri-dev]
description = "Run Tauri dev after initialization"
depends = ["init"]
run = "bun run tauri dev"
